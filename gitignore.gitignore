.gitignore에 명시된 파일이나 폴더는 git add, git commit 등의 명령으로도 Git이 추적하지 않음

주로 빌드 결과물, 캐시 파일, 설정 파일, 개인 정보가 담긴 파일 등을 제외할 때 사용



# 빌드 파일
/build/
*.o
*.elf
*.hex
*.bin

# IDE 관련
.vscode/
*.launch
*.log

# CubeMX 자동 생성 백업
*.ioc-backup

# OS 생성 파일
.DS_Store
Thumbs.db